version: '3.9'

services:
  bot:
    command: 'npm run dev'
    volumes:
      - '.:/usr/src/app'
      - '/usr/src/app/node_modules/'
    ports:
      - '9229:9229'

  # database:
  #   logging:
  #     driver: 'none'
